<h1>Lista de Productos</h1>
<ul>
    {{#each products}}
        <li>{{this.name}} - ${{this.price}}</li>
    {{/each}}
</ul>
<a href="/views/realtimeproducts" class="btn btn-primary">Agregar Productos</a>
